node_modules
dist
dist-single
dist-tauri
.DS_Store
*.local

# Tauri (Rust) build artifacts + generated schemas
src-tauri/target
src-tauri/gen

# Local tooling / machine-specific config (absolute paths)
.claude/

# Hardened-demo shared token — a SECRET the operator drops here on the laptop (copied from the Pi
# by a private channel). Served locally by vite; must NEVER be committed. See docs/DEMO_CONNECT.md.
public/pieeg-demo-token

# Local-only data + notes (not part of the app/deploy)
seed-edfs-archive/
# Vendored PiEEG-server source, kept locally as a protocol reference only — NOT part of
# REACT and must never be committed into this repo (separate project, ~11 MB).
PiEEG-server-main-extract/
PiEEG-server-main.zip
AUDIT-v13.md
AUDIT-v15.7-improvements.md
AUDIT-deid-export.md
AUDIT-live-acquire.md
